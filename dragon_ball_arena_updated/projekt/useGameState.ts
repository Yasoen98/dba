/**
 * @deprecated PLIK LEGACY — NIE UŻYWAĆ
 *
 * Ten plik to stary monolityczny store sprzed refaktoryzacji na slice'y.
 * Aktualny store znajduje się w: src/stores/rootStore.ts
 *
 * Ten plik NIE jest importowany przez żadną część aplikacji.
 * Można go bezpiecznie usunąć z repozytorium.
 *
 * Aby usunąć: git rm src/game/useGameState.ts
 */

export {};
